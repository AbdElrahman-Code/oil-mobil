/**
 * Brand assets shipped with the code, so the site has a logo even before the
 * shop uploads one in Site Settings → Brand (which always takes priority).
 */
export const BRAND_NAME = 'Drift'
export const BRAND_LOGO = '/brand/logo.png'
export const BRAND_MARK = '/brand/mark.png'
